class Rectangle{
    constructor(point, dWidth, dHeight, sprite, color, angle) {
        this.point = point;
        this.dWidth = dWidth; 
        this.dHeight = dHeight; 
        this.sprite = sprite;
        this.color = color;
        this.angle = angle;
    }
}